/** A type that can be undefined */
declare type Maybe<T> = T | undefined

/** A typed array */
declare type TypedArray = Float64Array | Float32Array | Int32Array | Uint8Array;

/** Radians */
declare type Rad = number;

/** Degrees */
declare type Deg = number;
